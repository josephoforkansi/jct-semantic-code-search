[project]
name = "jct-semantic-code-search"
version = "0.1.0"
description = "Semantic behavioral code search for Python"
requires-python = ">=3.10"

[project.dependencies]
sentence-transformers = "*"
numpy = "*"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"